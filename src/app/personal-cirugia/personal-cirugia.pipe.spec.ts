import { PersonalCirugiaPipe } from './personal-cirugia.pipe';

describe('PersonalCirugiaPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonalCirugiaPipe();
    expect(pipe).toBeTruthy();
  });
});
